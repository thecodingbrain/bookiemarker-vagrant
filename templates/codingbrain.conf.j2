description "Gunicorn application server handling codingbrain"

start on (filesystem)
stop on runlevel [016]

respawn
setuid vagrant
setgid vagrant
chdir {{ codingbrain_dir }}

exec gunicorn --workers 3 --bind 0.0.0.0:7220 config.wsgi:application